CREATE TABLE organizers (
                            id SERIAL PRIMARY KEY,
                            name VARCHAR(255),
                            description TEXT,
                            email VARCHAR(255),
                            logourl VARCHAR(255)
);